package com.jobverse

import android.app.Application

class JobverseApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize repositories, analytics, etc. (if any)
    }
}
